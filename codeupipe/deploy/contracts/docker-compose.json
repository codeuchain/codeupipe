{
  "id": "docker-compose",
  "name": "Docker Compose",
  "category": "container",
  "docs_url": "https://docs.docker.com/compose/how-tos/environment-variables/",
  "required_vars": [],
  "optional_vars": [],
  "naming": {
    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
    "description": "POSIX env var: letters, digits, underscores; must start with letter or underscore"
  },
  "limits": {
    "key_max_length": 4096,
    "value_max_length": 131072,
    "max_vars": null,
    "notes": "Same kernel-level limits as Docker. Compose interpolates ${VAR} from host env or .env file."
  },
  "secret_backends": ["env-file", "docker-secret", "hashicorp-vault"],
  "export_formats": ["env-file", "docker-compose-yaml"],
  "notes": "Compose supports environment: block (inline) or env_file: directive. Secrets via docker secrets."
}
