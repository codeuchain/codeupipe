{
  "id": "aws-app-runner",
  "name": "AWS App Runner",
  "category": "cloud",
  "docs_url": "https://docs.aws.amazon.com/apprunner/latest/dg/manage-configure.html",
  "required_vars": [],
  "optional_vars": [
    "PORT",
    "AWS_REGION"
  ],
  "naming": {
    "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
    "description": "Standard POSIX. AWS_ prefix vars are reserved by the runtime.",
    "forbidden_prefixes": [
      "AWS_"
    ]
  },
  "limits": {
    "key_max_length": 256,
    "value_max_length": 2048,
    "max_vars": 100,
    "max_total_size": null,
    "notes": "Max 100 env vars. Keys \u2264 256 chars. Values \u2264 2048 chars. Vars are set per service version."
  },
  "secret_backends": [
    "aws-ssm",
    "aws-secrets-manager"
  ],
  "export_formats": [
    "env-file",
    "ecs-task-def-json"
  ],
  "notes": "App Runner supports direct env var config and SSM Parameter Store references. Each deployment revision can have different env var configurations."
}
