"""codeupipe.ai.entry — CLI-level entry points for AI features.

These functions are invoked by the ``cup ai-*`` subcommands and
by integration tests that exercise the full CLI → pipeline flow.
"""

from __future__ import annotations
